# Copy this file to .env.local and fill in your values

# Required: FastAPI backend URL used by the Next.js processing route
FASTAPI_BASE_URL=http://127.0.0.1:8000

# Legacy variables kept only while the old direct-model path still exists.
# They are no longer needed once the app fully uses the FastAPI backend.
# OPENAI_API_KEY=sk-proj-...
# EVAL_PROMPT="Your custom prompt here..."
